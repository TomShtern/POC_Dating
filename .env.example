# POC Dating Application - Environment Variables Template
#
# PURPOSE: This file serves as a template for environment configuration across all services.
# Users should copy this to .env and populate with actual values.
#
# WHY THIS APPROACH:
# - Keeps sensitive data out of version control
# - Allows different configs per environment (dev/staging/prod)
# - Enables deployment flexibility without code changes
# - Follows 12-factor app principles
#
# ALTERNATIVES:
# - Hardcoded values in application.yml (NOT secure - secrets exposed)
# - Spring Cloud Config Server (overkill for POC, better for microservices at scale)
# - AWS Secrets Manager (only if deploying to AWS)
#
# RATIONALE: Environment variables are the Spring Boot standard and work everywhere
# (local dev, Docker containers, cloud platforms)

# ========================================
# APPLICATION CONFIGURATION
# ========================================

# Application environment: dev, staging, prod
APP_ENV=dev

# Application name for logging/identification
APP_NAME=POC_Dating

# ========================================
# DATABASE CONFIGURATION
# ========================================

# PostgreSQL Connection
POSTGRES_HOST=postgres
POSTGRES_PORT=5432
POSTGRES_DB=dating_db
POSTGRES_USER=dating_user
POSTGRES_PASSWORD=changeme123
POSTGRES_SSL_MODE=prefer

# Database connection pool size
DB_POOL_MIN_SIZE=5
DB_POOL_MAX_SIZE=20

# ========================================
# REDIS CACHE CONFIGURATION
# ========================================

REDIS_HOST=redis
REDIS_PORT=6379
REDIS_PASSWORD=
REDIS_TTL_MINUTES=30

# ========================================
# RABBITMQ MESSAGE BROKER
# ========================================

RABBITMQ_HOST=rabbitmq
RABBITMQ_PORT=5672
RABBITMQ_USER=guest
RABBITMQ_PASSWORD=guest
RABBITMQ_VHOST=/

# ========================================
# JWT SECURITY CONFIGURATION
# ========================================

# JWT secret key (min 256 bits / 32 bytes for HS256)
JWT_SECRET=change-me-with-a-secure-32-character-key-here
JWT_EXPIRATION_MS=86400000
JWT_REFRESH_EXPIRATION_MS=604800000

# ========================================
# MICROSERVICE PORTS
# ========================================

# Each service runs on a separate port internally
API_GATEWAY_PORT=8080
USER_SERVICE_PORT=8081
MATCH_SERVICE_PORT=8082
CHAT_SERVICE_PORT=8083
RECOMMENDATION_SERVICE_PORT=8084

# ========================================
# CORS CONFIGURATION
# ========================================

# Allowed origins for CORS (comma-separated)
CORS_ALLOWED_ORIGINS=http://localhost:3000,http://localhost:8080

# ========================================
# LOGGING CONFIGURATION
# ========================================

# Log level: DEBUG, INFO, WARN, ERROR
LOG_LEVEL=INFO

# ========================================
# EMAIL CONFIGURATION (FUTURE)
# ========================================

# SMTP server for notifications
SMTP_HOST=smtp.gmail.com
SMTP_PORT=587
SMTP_USER=your-email@gmail.com
SMTP_PASSWORD=your-app-password
SMTP_FROM=noreply@datingapp.com

# ========================================
# AWS/CLOUD CONFIGURATION (OPTIONAL)
# ========================================

# AWS credentials (if deploying to AWS)
AWS_REGION=us-east-1
AWS_ACCESS_KEY_ID=your-key-id
AWS_SECRET_ACCESS_KEY=your-secret-key

# ========================================
# FEATURE FLAGS (FUTURE)
# ========================================

# Enable/disable features for A/B testing
FEATURE_PREMIUM_MESSAGING=false
FEATURE_VIDEO_CHAT=false
FEATURE_LOCATION_BASED=true
